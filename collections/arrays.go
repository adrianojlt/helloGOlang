package main

import (
	"fmt"
)

func init() {
}

func main() {
	var array [5] int
}

func arrays() {

	var john User
	bill := new(User)
	bill.name = "BILL"
	fmt.Println(john.enabled)
	fmt.Println(bill.name)

	var initArray = []string{"2", "3"}
	fmt.Println(initArray)
}
